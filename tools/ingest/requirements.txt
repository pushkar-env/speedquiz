# Ingestion toolchain. Install into the backend venv:
#   backend/.venv/Scripts/python -m pip install -r tools/ingest/requirements.txt

# PDF layout, text geometry, figure extraction and page rendering.
pymupdf==1.28.2

# Figure normalisation and the dark-variant bake.
pillow>=11.0
numpy>=2.0

# The model client. Already a backend dependency; pinned here so the tool can
# be installed on its own.
httpx==0.28.1

# Only needed when INGEST_ASSET_STORE=r2.
boto3>=1.35 ; extra == "r2"
