# Replace speedquiz.app with your real domain before first deploy.
# Caddy obtains and renews Let's Encrypt certificates automatically.

speedquiz.app {
	encode gzip
	reverse_proxy api:8000
}

www.speedquiz.app {
	redir https://speedquiz.app{uri} permanent
}
